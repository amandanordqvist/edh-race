[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[dev]
  command = "npm run dev"
  targetPort = 5173

# React Router SPA: serve index.html for client routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
